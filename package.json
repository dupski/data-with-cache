{
  "name": "data-with-cache",
  "version": "1.0.0",
  "description": "Backend-agnostic caching library that provides caching strategies for application data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  },
  "scripts": {
    "build": "rm -rf ./dist && tslint -p . && tsc",
    "build-example": "tsc -m system -target ES6 -outFile example/client.js src/example/client.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cache",
    "caching",
    "offline",
    "api",
    "stale-while-revalidate",
    "strategy"
  ],
  "author": "Russell Briggs <russ@russellbriggs.co>",
  "license": "MIT"
}